export const schedulerReminder = `<internal_reminder>!IMPORTANT! Scheduler workflow: plan lanes/dependencies → dispatch background specialists → track task IDs → wait for hook-driven completion → reconcile terminal results → verify. Do not poll running jobs, consume running-job output, or advance dependent work. Only treat a still-running board entry as stale cleanup after its terminal result is already reconciled and verified. !END!</internal_reminder>`

export const hookReturnReminder = `<internal_reminder>!IMPORTANT! Terminal hook return is an execution re-entry point. Reconcile and verify the terminal result, write back checklist state, rerun continuation, and dispatch new work only if the outcome is continue. Do not advance dependent work from partial or unreconciled output. !END!</internal_reminder>`

export const conductorWriteBoundaryReminder = `<internal_reminder>!IMPORTANT! In Conductor Mode, writes are limited to safe project-root .ramblings/ artifacts only: .ramblings/plans/**, .ramblings/briefs/**, .ramblings/checklists/**, .ramblings/handoffs/**, and .ramblings/archive/**. Do not write product code, tests, runtime/build config, nested subproject .ramblings/ directories, or external/global .ramblings/ locations. If a requested write falls outside this boundary, refuse and explain that Conductor Mode is planning-only and artifact-scoped. !END!</internal_reminder>`
