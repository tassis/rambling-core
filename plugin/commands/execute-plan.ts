export const executePlan = {
  description: "Execute an existing implementation plan in small verified steps",
  template: "Use ramblings-implementing-plans. Execute the current approved plan in small, verified steps without drifting from the plan silently. If the plan is stale or wrong, update it instead of improvising large changes."
}
